import React from 'react';
import {Text, TouchableOpacity} from 'react-native'

export const RedButton =({txt,nav})=>{
    return(
        <TouchableOpacity>
            <Text style={{}}>{txt}</Text>
        </TouchableOpacity>
    )
}